<UserControl x:Class="DocumentManagement.Wpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:conv="clr-namespace:DocumentManagement.Wpf.Converters"
             Background="{StaticResource AppBackgroundBrush}">

    <UserControl.Resources>
        <conv:StatusNameToBrushConverter x:Key="StatusNameToBrushConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <Style x:Key="MetricCardStyle" TargetType="Border" BasedOn="{StaticResource AppCardStyle}">
            <Setter Property="Padding" Value="18,16"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
        </Style>

        <Style x:Key="GridTextStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="GridMutedTextStyle" TargetType="TextBlock" BasedOn="{StaticResource GridTextStyle}">
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="TỔNG QUAN"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="{StaticResource TextBrush}"
                           Margin="0,0,0,16"/>

                <UniformGrid Columns="5" Margin="0,0,0,14">
                    <Border Style="{StaticResource MetricCardStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="42"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="34" Height="34" CornerRadius="17" Background="#087A3D">
                                <materialDesign:PackIcon Kind="FileDocumentOutline" Width="18" Height="18" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                <TextBlock Text="Tổng số văn bản" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                                <TextBlock Text="{Binding TotalDocuments}" FontSize="25" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,2,0,0"/>
                                <TextBlock Text="+12,5% so với tháng trước" FontSize="10" Foreground="{StaticResource SuccessBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource MetricCardStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="42"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="34" Height="34" CornerRadius="17" Background="#F59E0B">
                                <materialDesign:PackIcon Kind="ClipboardTextOutline" Width="18" Height="18" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                <TextBlock Text="Đã ban hành" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                                <TextBlock Text="{Binding IssuedDocuments}" FontSize="25" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,2,0,0"/>
                                <TextBlock Text="-0,7% so với tháng trước" FontSize="10" Foreground="#F59E0B"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource MetricCardStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="42"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="34" Height="34" CornerRadius="17" Background="#087A3D">
                                <materialDesign:PackIcon Kind="FileCheckOutline" Width="18" Height="18" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                <TextBlock Text="Có hiệu lực" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                                <TextBlock Text="{Binding EffectiveDocuments}" FontSize="25" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,2,0,0"/>
                                <TextBlock Text="+10,3% so với tháng trước" FontSize="10" Foreground="{StaticResource SuccessBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource MetricCardStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="42"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="34" Height="34" CornerRadius="17" Background="#E30613">
                                <materialDesign:PackIcon Kind="FileCancelOutline" Width="18" Height="18" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                <TextBlock Text="Hết hiệu lực" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                                <TextBlock Text="{Binding ExpiredDocuments}" FontSize="25" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,2,0,0"/>
                                <TextBlock Text="-5,2% so với tháng trước" FontSize="10" Foreground="{StaticResource DangerBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource AppCardStyle}" Padding="18,16">
                        <StackPanel>
                            <TextBlock Text="TB/thang" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                            <TextBlock Text="{Binding AverageIssuedPerMonth, StringFormat={}{0:N1}}" FontSize="25" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,2,0,0"/>
                            <TextBlock Text="Văn bản ban hành" FontSize="10" Foreground="{StaticResource TextMutedBrush}"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.05*"/>
                        <ColumnDefinition Width="1.55*"/>
                        <ColumnDefinition Width="1.25*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource AppCardStyle}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="Cơ cấu hiệu lực" FontSize="13" FontWeight="Bold" Margin="0,0,0,10"/>
                            <Grid Height="230">
                                <lvc:PieChart Series="{Binding EffectivenessSeries}" LegendLocation="Right" HoverPushOut="6"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource AppCardStyle}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="Văn bản ban hành theo tháng" FontSize="13" FontWeight="Bold" Margin="0,0,0,10"/>
                            <Grid Height="230">
                                <lvc:CartesianChart Series="{Binding MonthlyIssuedSeries}" LegendLocation="Top">
                                    <lvc:CartesianChart.AxisX>
                                        <lvc:Axis Labels="{Binding MonthlyIssuedLabels}" Separator="{x:Static lvc:DefaultAxes.CleanSeparator}"/>
                                    </lvc:CartesianChart.AxisX>
                                    <lvc:CartesianChart.AxisY>
                                        <lvc:Axis MinValue="0"/>
                                    </lvc:CartesianChart.AxisY>
                                </lvc:CartesianChart>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource AppCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Văn bản theo phòng ban" FontSize="13" FontWeight="Bold" Margin="0,0,0,10"/>
                            <Grid Height="230">
                                <lvc:CartesianChart Series="{Binding DepartmentIssuedSeries}" LegendLocation="None">
                                    <lvc:CartesianChart.AxisX>
                                        <lvc:Axis Labels="{Binding DepartmentIssuedLabels}" Separator="{x:Static lvc:DefaultAxes.CleanSeparator}"/>
                                    </lvc:CartesianChart.AxisX>
                                    <lvc:CartesianChart.AxisY>
                                        <lvc:Axis MinValue="0"/>
                                    </lvc:CartesianChart.AxisY>
                                </lvc:CartesianChart>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.55*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource AppCardStyle}" Padding="0" Margin="0,0,12,0">
                        <StackPanel>
                            <DockPanel Margin="18,14,18,8">
                                <TextBlock Text="Văn bản mới ban hành" FontSize="13" FontWeight="Bold" DockPanel.Dock="Left"/>
                                <TextBlock Text="Xem tất cả" FontSize="11" Foreground="{StaticResource PrimaryBrush}" DockPanel.Dock="Right"/>
                            </DockPanel>

                            <DataGrid x:Name="RecentDocumentsGrid"
                                      ItemsSource="{Binding RecentDocuments}"
                                      Style="{StaticResource AppDataGridStyle}"
                                      AutoGenerateColumns="False"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="None"
                                      RowHeaderWidth="0"
                                      RowHeight="42"
                                      ColumnHeaderHeight="36"
                                      MouseDoubleClick="RecentDocumentsGrid_MouseDoubleClick">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Số ký hiệu" Binding="{Binding DocumentNumber}" Width="110" ElementStyle="{StaticResource GridMutedTextStyle}"/>
                                    <DataGridTextColumn Header="Trích yếu" Binding="{Binding Title}" Width="*" ElementStyle="{StaticResource GridTextStyle}"/>
                                    <DataGridTextColumn Header="Cơ quan ban hành" Binding="{Binding SenderName}" Width="140" ElementStyle="{StaticResource GridMutedTextStyle}"/>
                                    <DataGridTextColumn Header="Ngày ban hành" Binding="{Binding IssueDate}" Width="110" ElementStyle="{StaticResource GridMutedTextStyle}"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource AppCardStyle}">
                        <StackPanel>
                            <DockPanel Margin="0,0,0,10">
                                <TextBlock Text="Công việc chờ xử lý" FontSize="13" FontWeight="Bold" DockPanel.Dock="Left"/>
                                <TextBlock Text="Xem tất cả" FontSize="11" Foreground="{StaticResource PrimaryBrush}" DockPanel.Dock="Right"/>
                            </DockPanel>

                            <ItemsControl ItemsSource="{Binding RecentDocuments}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{StaticResource BorderBrushDefault}"
                                                BorderThickness="0,0,0,1"
                                                Padding="0,10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="34"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Width="24" Height="24" CornerRadius="12" Background="#FFF7ED">
                                                    <materialDesign:PackIcon Kind="AccountOutline" Width="15" Height="15" Foreground="#F59E0B" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="{Binding Title}" FontSize="12" Foreground="{StaticResource TextBrush}" TextTrimming="CharacterEllipsis"/>
                                                    <TextBlock Text="{Binding ProcessingDepartment}" FontSize="10" Foreground="{StaticResource TextMutedBrush}" Margin="0,3,0,0"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="2" Text="{Binding UpdatedAt, StringFormat=dd/MM}" FontSize="10" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Top"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <Grid Style="{StaticResource LoadingOverlayGridStyle}"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
            <Border Style="{StaticResource LoadingCardStyle}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar Width="140" Height="6" IsIndeterminate="True" Margin="0,0,0,18"/>
                    <TextBlock Text="Đang tải dashboard..." Style="{StaticResource LoadingTextStyle}"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Style="{StaticResource ErrorBannerStyle}"
                Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="{Binding ErrorMessage}" Style="{StaticResource ErrorBannerTextStyle}"/>
        </Border>
    </Grid>
</UserControl>
