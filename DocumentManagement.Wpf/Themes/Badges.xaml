<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="FluentBadgeBorderStyle" TargetType="Border">
        <Setter Property="Height" Value="18"/>
        <Setter Property="MinWidth" Value="56"/>
        <Setter Property="Padding" Value="6,0"/>
        <Setter Property="CornerRadius" Value="9"/>
        <Setter Property="Background" Value="{StaticResource FluentSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource FluentBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style x:Key="FluentBadgeTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeCaption}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource FluentTextMutedBrush}"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Center"/>
    </Style>

    <Style x:Key="FluentStatusPillStyle" TargetType="Border" BasedOn="{StaticResource FluentBadgeBorderStyle}">
        <Setter Property="Background" Value="{StaticResource FluentBrandSoftBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource FluentBrandBrush}"/>
    </Style>

</ResourceDictionary>
